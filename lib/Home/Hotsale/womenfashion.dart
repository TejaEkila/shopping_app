import 'package:flutter/material.dart';

class WomensFashion extends StatelessWidget {
  const WomensFashion({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.amber,
    );
  }
}