import 'package:flutter/material.dart';

class WomensCasual extends StatelessWidget {
  const WomensCasual({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.amber,
    );
  }
}