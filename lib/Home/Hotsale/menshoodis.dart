import 'package:flutter/material.dart';

class MensHoodies extends StatelessWidget {
  const MensHoodies({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.amber,
    );
  }
}