import 'package:flutter/material.dart';

class GirlsTops extends StatelessWidget {
  const GirlsTops({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.amber,
    );
  }
}